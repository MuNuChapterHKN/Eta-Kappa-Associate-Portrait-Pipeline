# Pinned with lower bounds to stay compatible with Python 3.11 → 3.14.
# If you need reproducible builds pin to exact versions that ship wheels
# for your target Python (run `pip freeze > requirements.lock.txt`).

streamlit>=1.56.0
mediapipe>=0.10.30
rembg>=2.0.59
onnxruntime>=1.19.2
opencv-python-headless>=4.10.0.84
pillow>=10.4.0
numpy>=1.26.4
pymatting>=1.1.15
scikit-image>=0.26.0